[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "threads-bot"
version = "0.1.0"
description = "Threads (threads.net) への自動投稿ボット。投稿キューを GitHub Actions の cron で消化する。"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
threads-bot = "threads_bot.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
